server:
  port: 8081

back:
  server:
    uri: http://localhost:8080

spring:
  application:
    name: "NC_EDU_SD2_Ilya_Prishchepa"

security:
  config:
    signinkey: "SubscriptionBD"
    prefix: "Bearer "
    header: "Authorization"
    authoritieskey: "scopes"
    token:
      validityseconds: 18000
